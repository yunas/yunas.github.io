// Generated by CoffeeScript 1.6.2
var next, previous, resize;

$(function() {
  setTimeout((function() {
    resize();
    $('.next').mouseover(function() {
      return $('img', $(this)).attr('src', 'assets/img/nav/down_blue.png');
    });
    $('.next').mouseout(function() {
      return $('img', $(this)).attr('src', 'assets/img/nav/down_white.png');
    });
    $('.previous').mouseover(function() {
      return $('img', $(this)).attr('src', 'assets/img/nav/up_blue.png');
    });
    $('.previous').mouseout(function() {
      return $('img', $(this)).attr('src', 'assets/img/nav/up_white.png');
    });
    $('.scroll-down').data('shown', '1');
    $('.overlay').click(function() {
      return $('.overlay').fadeOut();
    });
    $('.message').click(function() {
      return $('.overlay').fadeOut();
    });
    $(window).scroll(function() {
      var current;

      if ($('body').scrollTop() < 100) {
        if ($('.scroll-down').data('shown') === '0') {
          $('.scroll-down').fadeIn();
          $('.scroll-down').data('shown', '1');
        }
      } else {
        console.log('here');
        if ($('.scroll-down').data('shown') === '1') {
          $('.scroll-down').fadeOut();
          $('.scroll-down').data('shown', '0');
        }
      }
      current = Math.floor($('body').scrollTop() / $(window).height());
      $('nav ul li').removeClass('selected');
      if (current <= 0) {

      } else if (current <= 1) {
        return $('nav ul li:eq(0)').addClass('selected');
      } else if (current < 7) {
        return $('nav ul li:eq(1)').addClass('selected');
      } else {
        return $('nav ul li:eq(2)').addClass('selected');
      }
    });
    $('.next').click(next);
    $('.previous').click(previous);
    return $('nav ul li').click(function() {
      $('nav ul li').removeClass('selected');
      $(this).addClass('selected');
      $('body').animate({
        scrollTop: $('#' + $(this).data('anc')).offset().top
      }, 500);
      return false;
    });
  }), 500);
});

next = function() {
  var current;

  current = Math.floor($('body').scrollTop() / $(window).height());
  $('body').animate({
    scrollTop: (current + 1) * $(window).height()
  }, 1000);
};

previous = function() {
  var current;

  current = Math.floor($('body').scrollTop() / $(window).height());
  $('body').animate({
    scrollTop: (current - 1) * $(window).height()
  }, 1000);
};

resize = function() {
  $(window).scrollTop(0);
  $('nav').width($(window).width());
  $('nav').height(80);
  $('nav ul').css('margin-left', ($(window).width() - 330) / 2);
  $('.logo-holder').css('top', ($(window).height() - 166) / 2);
  $('.logo-holder').css('left', ($(window).width() - 205) / 2);
  $('.scroll-down').css('left', ($(window).width() - 59) / 2);
  $('.scroll-down').css('bottom', 5);
  $('.page').each(function() {
    $(this).width($(window).width());
    return $(this).height($(window).height());
  });
  $('.about-holder').each(function() {
    $(this).css('left', ($(window).width() - 770) / 2);
    return $(this).css('top', ($(window).height() - 400) / 2);
  });
  $('.reel-holder').each(function() {
    $(this).css('left', ($(window).width() - $(this).width()) / 2);
    return $(this).css('top', ($(window).height() - $(this).height()) / 2);
  });
  $('.contact-list-holder').each(function() {
    $(this).css('left', ($(window).width() - $(this).width()) / 2);
    return $(this).css('top', ($(window).height() - 400) / 2);
  });
  $('.control').css('right', 15);
  $('.control').css('top', ($(window).height() - 60) / 2);
  $('.overlay').width($(window).width());
  $('.overlay').height($(window).height());
  $('.message').css('left', ($(window).width() - 280) / 2);
  $('.message').css('top', ($(window).height() - 200) / 2);
};
